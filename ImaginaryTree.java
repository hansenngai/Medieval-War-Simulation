import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Behind every tree, there is an imaginary tree (smaller than the original tree) located at the same spot as the original tree.
 * The reason behind imaginary tree is because the arrow sometimes get stuck on the edge of the "box" surrounding the tree (appears as if the arrow
 * stopped in the middle of the air). Thus imaginary tree has its "box" located inside the original tree and nothing seems unusual.
 * 
 * @author (Ramy Elbakari) 
 * @version (0.0.5)
 */
public class ImaginaryTree extends Actor
{
    /**
     * Act - do whatever the ImaginaryTree wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
